services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: prop_agent
    ports:
      - "5432:5432"
    volumes:
      - prop_agent_pg:/var/lib/postgresql/data

volumes:
  prop_agent_pg:
